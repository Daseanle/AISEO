{
  "name": "@acme/worker",
  "private": true,
  "version": "0.1.0",
  "description": "Background worker (placeholder)",
  "type": "module",
  "scripts": {
    "dev": "node worker.js",
    "start": "node worker.js",
    "lint": "echo 'No linting for worker yet'",
    "test": "echo 'No tests for worker yet'"
  }
}
